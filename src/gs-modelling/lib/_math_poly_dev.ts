import * as gs from "gs-json";
import * as three from "three";
import * as threex from "./_three_utils_dev";

/**
 * Function to check a point is on a plane.
 * The plane is represented by an origin and a normal.
 */
export function _pointIsOnPlane(origin: gs.IPoint, normal1: number[], point: gs.IPoint): boolean {

    throw new Error("Method not implemented.");

}
