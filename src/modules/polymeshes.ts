import * as model_interfaces from "../libs/gs_json/utils/model_interfaces";

//this is just an example
export class Polymesh {
	public static makePolygon(points:number[][]):any {
		return {points:points};
	}
	//lots more to come here
}